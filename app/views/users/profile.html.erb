<% if current_user.pending_friendships.exists?(@user.id) %>
<p class="btn btn-success disabled"> Pending </p>
<% else%>
<p class="px-2">
  <%= link_to 'Invite to Friend', user_friendships_path(@user), class:"btn btn-secondary", method: :post %>
</p>
<!--- <% if @user.friend?(current_user) == false %>
<%= link_to 'Accept', user_friendship_path(@user, current_user), class:"btn btn-outline-primary", method: :patch %>
<%= link_to 'Deny', user_friendships_path(@user) , class:"btn btn-outline-secondary" %>
<% end %> --->
<% end %>


<article class="timeline">
  <h3 class="text-primary"><%= @user.name.capitalize %> Posts:</h3>
  <ul class="posts">
    <%= render @posts %>
  </ul>
</article>